# Uncomment this line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!

target 'FlightProject' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
    pod 'Alamofire', '~> 4.4.0'
    pod 'CVCalendar', '~> 1.6.0'
    pod 'Kingfisher', '~> 4.0'

  target 'FlightProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FlightProjectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
